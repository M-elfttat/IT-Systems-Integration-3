FLASK_ENV=development
FLASK_DEBUG=1

# For local dev, keep this in a real .env (not committed)
SECRET_KEY=change-me
JWT_SECRET_KEY=change-me-too

DATABASE_URL=postgresql+psycopg://sysint_user:sysint_password@localhost:5432/sysint_db

# React dev server origin (Phase 2 frontend)
CORS_ORIGINS=http://localhost:5173

